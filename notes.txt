first code ( in python form condition)
if request.method == "POST"
	form = UserCreationForm(request.POST)
	if form.is_valid():
		form.save()
		username = form.cleaned_data['username']
		message.success(request, 'this is message')
		return redirect('home')
		
else:
	form = UserCreationForm()

return render(request, 'login')




notes :- 
- ask about profits & loses
- push the website on github ===
- logout button customize ===
- kiwi image customize 
- make admin panel arabic
- customize admin panel themes
- start looking for register user form methods 
- search hosting websites 
- load requirements file
- search about local db and online db what is better and faster